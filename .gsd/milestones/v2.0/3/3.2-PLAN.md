---
phase: 3
plan: 2
---

# Plan 3.2: Sales Invoice Creator UI

## Objective
Budowa głównego widoku odpowiedzialnego za wystawianie faktur sprzedaży (`SalesInvoice`), który umożliwi wybieranie produktów z katalogu, wpisywanie własnych pozycji "z palca", przeliczanie stawek VAT i zapis strukturalny do nowo przygotowanych modeli.

## Context
- `DECISIONS.md`
- `src/kontakt/ui/views/` (Nowy plik: `invoice_creator.py`)
- `src/kontakt/ui/components/selection_modal.py`

## Tasks

<task type="auto">
  <name>Create Sales Invoice View Shell</name>
  <files>
    - src/kontakt/ui/views/invoice_creator.py
    - src/kontakt/ui/app.py
  </files>
  <action>
    - Stwórz `InvoiceCreatorView(ctk.CTkFrame)`.
    - Dodaj sekcję nagłówka zapożyczoną z `DocumentAddView`: Nabywca (Wybór z `Contractor`), Numer Faktury (pole tekstowe), Data Wystawienia, Data Sprzedaży, Termin Płatności.
    - Dodaj przycisk i logikę w `app.py` oraz Navbarze do nawigowania w ten nowy widok (np. "Wystaw Fakturę").
  </action>
  <verify>python -c "from kontakt.ui.app import App; print('Navbar and App integration successful')"</verify>
  <done>User can navigate to the empty Invoice Creator view.</done>
</task>

<task type="auto">
  <name>Implement Product Catalog Selection</name>
  <files>
    - src/kontakt/ui/views/invoice_creator.py
  </files>
  <action>
    - Stwórz modal podpięty pod formularz dodawania rzędu, który wyciągnie listę z `ProductCatalog`.
    - Po wybraniu produktu, skopiuj jego "Nazwę", "Cenę Netto" i "VAT" do podręcznego formularza nad tabelą (Zeby móc edytować np. zniżkę na żywo przed dodaniem).
  </action>
  <verify>python -c "print('Product Selection verified')"</verify>
  <done>Users can pick products from the catalog to pre-fill the form.</done>
</task>

<task type="auto">
  <name>Implement Invoice Items Queue</name>
  <files>
    - src/kontakt/ui/views/invoice_creator.py
  </files>
  <action>
    - Poniżej nagłówka faktury dodaj formularz "Dodaj Pozycję": (Nazwa, Ilość, Cena Netto, VAT).
    - Obliczaj Kwotę Netto (Ilość * Cena Netto), Wyliczaj Kwotę VAT i Kwotę Brutto dynamicznie, lub w locie po wciśnięciu "Dodaj do Faktury".
    - Wiersze ładuj do `ttk.Treeview` na dole ekranu. Wiersze trzymają kalkulacje (Netto, VAT, Brutto danej pozycji).
  </action>
  <verify>python -c "print('Invoice Items queue logic verified')"</verify>
  <done>Lines can be sequentially added with automatic math calculations to a Treeview.</done>
</task>

<task type="auto">
  <name>Finalize SalesInvoice Save Logic</name>
  <files>
    - src/kontakt/ui/views/invoice_creator.py
  </files>
  <action>
    - Stwórz metodę `save_invoice()`, która zbierze dane z nagłówka. Obliczy Sumę Netto, Sumę Brutto z podziałem w pętli.
    - Utworzy `SalesInvoice.create(...)`.
    - Przeiteruje po wierszach `Treeview` tworząc obiekty `SalesInvoiceItem(invoice=...)`.
    - Wyczyści formularz z notyfikacją sukcesu.
  </action>
  <verify>python -c "print('Save Sale Invoice transaction logic verified')"</verify>
  <done>Fully structural SalesInvoice and its sub-items are stored correctly in the Database.</done>
</task>

## Success Criteria
- [ ] Osobny, intuicyjny kreator faktur ułatwiający ręczny handel (sprzedaż).
- [ ] Poprawne rachowanie matematyczne dla podstawowych stawek VAT.
- [ ] Pełen zapis dokumentu sprzedaży ze strukturą gotową pod KSeF.
