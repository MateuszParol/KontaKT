# Plan 3.2 Summary

- **Completed Task 1:** Created `InvoiceCreatorView` and added the "Wystaw Fakturę" route to the Navbar.
- **Completed Task 2:** Implemented `ProductCatalog` connection. Clicking "Wybierz z Katalogu" opens a modal filtering products. Selecting one pre-fills the Name, Net Price, and VAT fields.
- **Completed Task 3:** Added dynamic Queue adding with `add_item_to_queue`. The script attempts to parse inputs, calculates `total_net` and `total_gross` correctly depending on the typed VAT rate, and updates the running `update_totals` label.
- **Completed Task 4:** Finalized `save_invoice()`, ensuring date validations, contractor bindings, and transactionally creating both the `SalesInvoice` and iterating over the Treeview for `SalesInvoiceItem`s. Form resets on success.
