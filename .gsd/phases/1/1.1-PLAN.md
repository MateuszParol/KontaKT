---
phase: 1
plan: 1
---

# Plan 1.1: ThemeManager & TokyoNight Palette

## Objective
Utworzenie centralnej instancji obslugujacej motywy (Light i Tokyo Night Dark). `ThemeManager` będzie dostarczał spójnego zestawu kolorów (background, foreground, accent, text) dla wszystkich elementów Tkinter'a i CustomTkinter'a w aplikacji, uniezależniając nas od domyślnych niebieskich/szarych barw biblioteki.

## Context
- `ROADMAP.md` (Phase 1)
- `src/kontakt/ui/theme.py` (Nowy plik)

## Tasks

<task type="auto">
  <name>Create Theme Registry (Tokyo Night)</name>
  <files>
    - src/kontakt/ui/theme.py
  </files>
  <action>
    - Utwórz klasę `ThemeManager` wykorzystującą narzucone kolory "Tokyo Night" (np. tło ciemne `#1a1b26`, tło jaśniejsze `#24283b`, fiolety `#bb9af7`, błękity `#7aa2f7`, zieleń `#9ece6a`, biel tekstowa `#c0caf5`).
    - Skonfiguruj motyw jasny ("Light") w stonowanych, pastelowych barwach, by był czytelny w dzień.
    - Metoda `get_color(name)` powinna zwracać odpowiedniego HEXa zależnie od obecnie wybranego trybu (`self.current_mode="dark"`).
    - Zastosuj wzorzec Singleton, by `ThemeManager` mógł być łatwo importowany z innych plików UI bez żonglowania instancjami.
  </action>
  <verify>python -c "from kontakt.ui.theme import ThemeManager; t=ThemeManager(); print(t.get_color('bg_main'))"</verify>
  <done>ThemeManager exists, holds both mode palettes, and exposes a clean color getter.</done>
</task>

## Success Criteria
- [ ] Zbudowany, odseparowany plik definiujący paletę ułatwiający przyszłe zmiany stylowe całej aplikacji.
