---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: UI Performance Optimization

## Objective
Limit data rendering to Top 100 records and add search filtering to prevent application lag on large datasets.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- src/kontakt/ui/views/accounts.py
- src/kontakt/ui/views/contractors.py
- src/kontakt/ui/views/history.py

## Tasks

<task type="auto">
  <name>Implement Top 100 limit and Search Filtering</name>
  <files>
    src/kontakt/ui/views/accounts.py
    src/kontakt/ui/views/contractors.py
    src/kontakt/ui/views/history.py
  </files>
  <action>
    - Ensure `AccountsView`, `ContractorsView`, and `HistoryView` load a maximum of 100 records using Peewee's `.limit(100)`.
    - Ensure search fields are present in the headers and trigger refined DB queries.
    - Check if the code already has these optimizations (as suggested by earlier codebase mapping). If so, functionally verify the logic and consider the task complete.
  </action>
  <verify>grep -A 2 "limit(100)" src/kontakt/ui/views/accounts.py</verify>
  <done>Lists are restricted to 100 items and a search box filters the respective Peewee queries without performance drops.</done>
</task>

## Success Criteria
- [ ] Accounts, Contractors, and History views render max 100 records.
- [ ] Search fields actively filter results based on user input.
