# Plan 2.1 Summary

- **Completed Task 1:** Implemented `_on_theme_toggle` callback inside `src/kontakt/ui/views/navbar.py` attached to a newly manufactured `CTKButton`.
- **Completed Task 2:** The newly born toggle handles modifying the inner state of the `ThemeManager` (`toggle_mode`).
- **Completed Task 3:** Established a callback mechanism bridging the Navbar to the main window (`App`), specifically transmitting the actual origin coordinates (X, Y) relative to the screen, serving as an epicenter groundwork for the proceeding radial animation.
