---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Python-Side Search Filtering & NIP Excel Import Fix

## Objective
Poprawa ergonomii wyszukiwania poprzez ignorowanie myślników i spacji w wpisywanych frazach (np. NIP, nr konta). Dodatkowo naprawa importu NIP z Excela (zapobieganie zapisowi np. `1234567890.0` wywoływanego przez konwersję floatów przez `pandas`).

## Context
- `DECISIONS.md`
- `src/kontakt/ui/views/document_add.py`
- `src/kontakt/services/importer.py`

## Tasks

<task type="auto">
  <name>Filter accounts and contractors in Python</name>
  <files>
    - src/kontakt/ui/views/document_add.py
  </files>
  <action>
    - W funkcjach `fetch_contractors` oraz `fetch_accounts` najpierw pobierz wszystkie rekordy korzystając z bazy `Contractor.select()` / `Account.select()`.
    - Jeśli użyta jest `phrase`, znormalizuj ją w Pythonie (np. `clean_phrase = phrase.replace("-", "").replace(" ", "").lower()`).
    - Następnie odfiltruj pobrane kolekcje sprawdzając, czy oczyszczona fraza znajduje się w oczyszczonym numerze NIP/NRP lub nazwie/symbolu rekordu (np. `if clean_phrase in account.symbol.replace("-", "").lower()`).
    - Zwróć tylko pasującą listę.
  </action>
  <verify>python -c "from kontakt.ui.views.document_add import DocumentAddView; print('Filtering logic can be parsed')"</verify>
  <done>Python-side search filtering handles hyphens naturally.</done>
</task>

<task type="auto">
  <name>Fix float parsing in NIP Import</name>
  <files>
    - src/kontakt/services/importer.py
  </files>
  <action>
    - W `import_contractors_from_excel`, zmodyfikuj linię odpowiedzialną za odczyt pliku dodając wymuszenie typu pola NIP jako string: `df = pd.read_excel(filepath, engine=engine, dtype={'NIP': str})` (oraz zabezpiecz się przed ewentualnym błędem jeśli kolumna nie ma typu na twardo używając odpowiedniego if-a do wycinania rezydualnego `.0`).
    - Najlepiej dla pewności wyciąć ewentualne rozszerzenie `.0` jeżeli po parsowaniu jako string wciąż występuje: `if nip.endswith('.0'): nip = nip[:-2]`.
  </action>
  <verify>python -c "from kontakt.services.importer import import_contractors_from_excel; print('Importer parsed')"</verify>
  <done>NIP string conversion properly handled.</done>
</task>

## Success Criteria
- [ ] Szukając ujęcia z modala z frazą "130-01" oraz "13001" wynik będzie ignorował myślnik.
- [ ] Import Excel bez `.0` na końcu stringa w przypadku NIP.
