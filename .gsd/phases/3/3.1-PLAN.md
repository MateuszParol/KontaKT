---
phase: 3
plan: 1
---

# Plan 3.1: Database Schemas for Sales Invoices and Catalog

## Objective
Stworzenie niezależnych modeli danych (`SalesInvoice`, `SalesInvoiceItem`, `ProductCatalog`) wspierających fakturowanie z dokładnością do wymogów podatkowych i ułatwiających przyszłą integrację eksportu KSeF.

## Context
- `DECISIONS.md`
- `src/kontakt/database/models.py`
- `src/kontakt/database/init_db.py`

## Tasks

<task type="auto">
  <name>Create ProductCatalog Model</name>
  <files>
    - src/kontakt/database/models.py
  </files>
  <action>
    - Dodaj nową klasę `ProductCatalog(BaseModel)`.
    - Atrybuty: `name` (CharField), `price_net` (DecimalField), `vat_rate` (IntegerField lub CharField podający `23`, `8`, `0`), `unit` (CharField np. "szt.", "usługa").
  </action>
  <verify>python -c "from kontakt.database.models import ProductCatalog; print('ProductCatalog defined')"</verify>
  <done>ProductCatalog model is accurately defined within Peewee schemas.</done>
</task>

<task type="auto">
  <name>Create SalesInvoice and Items Models</name>
  <files>
    - src/kontakt/database/models.py
  </files>
  <action>
    - Dodaj `SalesInvoice(BaseModel)` z polami: 
      - `number` (Char)
      - `date_issue` (Date)
      - `date_sale` (Date)
      - `due_date` (Date)
      - `contractor` (ForeignKey do Contractor)
      - `payment_method` (Char np. "przelew")
    - Dodaj `SalesInvoiceItem(BaseModel)` z polami:
      - `invoice` (ForeignKey do SalesInvoice)
      - `product_name` (Char - kopiujemy nazwę aby zmian w katalogu w przyszłości nie rzutowały na historyczne faktury)
      - `quantity` (DecimalField do ułamków lub IntegerField)
      - `price_net` (DecimalField)
      - `vat_rate` (Integer/Decimal)
      - `total_net` (DecimalField)
      - `total_gross` (DecimalField)
  </action>
  <verify>python -c "from kontakt.database.models import SalesInvoice, SalesInvoiceItem; print('SalesModels defined')"</verify>
  <done>Sales invoice structural models are added to `models.py` independent of ledger Documents.</done>
</task>

<task type="auto">
  <name>Update init_db Scripts</name>
  <files>
    - src/kontakt/database/init_db.py
  </files>
  <action>
    - Dodaj nowo utworzone tabele do listy `database.create_tables([...], safe=True)`.
    - Spraw, by `init_db.py` bezpiecznie modyfikował schemat jeśli zajdzie taka potrzeba.
  </action>
  <verify>python src/kontakt/database/init_db.py</verify>
  <done>Database correctly registers the new tables without exceptions.</done>
</task>

## Success Criteria
- [ ] Istnieją odseparowane tabele faktur sprzedaży, ułatwiające KSeF i zwalniające nas z dbania o powiązania z księgowością systemową.
- [ ] Funkcjonujący model Asortymentu z przypisanym VAT i ceną jednostkową netto.
